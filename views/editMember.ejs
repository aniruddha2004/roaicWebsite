<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Member</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1>Edit Member</h1>
    <form action="/members/edit/<%= memberId %>" method="POST">
      <div class="form-group">
        <label for="name">Name</label>
        <input type="text" class="form-control" id="name" name="name" value="<%= member.name %>" required>
      </div>
      <div class="form-group">
        <label for="email">Email</label>
        <input type="email" class="form-control" id="email" name="email" value="<%= member.email %>" required>
      </div>
      <div class="form-group">
        <label for="role">Role</label>
        <select class="form-control" id="role" name="role" required>
          <option value="coordinator" <%= member.role === 'coordinator' ? 'selected' : '' %>>Coordinator</option>
          <option value="project manager" <%= member.role === 'project manager' ? 'selected' : '' %>>Project Manager</option>
          <option value="treasurer" <%= member.role === 'treasurer' ? 'selected' : '' %>>Treasurer</option>
          <option value="project member" <%= member.role === 'project member' ? 'selected' : '' %>>Project Member</option>
          <option value="non project member" <%= member.role === 'non project member' ? 'selected' : '' %>>Non-Project Member</option>
          <option value="faculty coordinator" <%= member.role === 'faculty coordinator' ? 'selected' : '' %>>Faculty Coordinator</option>
          <option value="guest" <%= member.role === 'guest' ? 'selected' : '' %>>Guest</option>
        </select>
      </div>
      <div class="form-group">
        <label for="program">Program</label>
        <input type="text" class="form-control" id="program" name="program" value="<%= member.program %>" required>
      </div>
      <div class="form-group">
        <label for="start">Start Year</label>
        <input type="number" class="form-control" id="start" name="start" value="<%= member.start %>" required>
      </div>
      <div class="form-group">
        <label for="end">End Year</label>
        <input type="number" class="form-control" id="end" name="end" value="<%= member.end %>" required>
      </div>
      <div class="form-group form-check">
        <input type="checkbox" class="form-check-input" id="admin" name="admin" <%= member.admin ? 'checked' : '' %>>
        <label class="form-check-label" for="admin">Make user an admin</label>
      </div>
      <button type="submit" class="btn btn-success">Update Member</button>
    </form>
  </div>
</body>
</html>
